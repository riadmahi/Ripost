package com.app.ripost.utils.models

data class Message(val messageID: String,
                   val message: String,
                   val dateCreated: String,
                   val sendBy: String) {
}